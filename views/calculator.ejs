<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🚀 Calculator with Database - CI/CD Demo</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        
        .header .badge {
            background: rgba(255,255,255,0.2);
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9em;
            display: inline-block;
            margin: 5px;
        }
        
        .main-content {
            display: flex;
            min-height: 500px;
        }
        
        .calculator-section {
            flex: 1;
            padding: 40px;
            background: #f8f9fa;
        }
        
        .calculator-form {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .form-row {
            display: flex;
            gap: 10px;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
        }
        
        .form-input {
            padding: 15px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 18px;
            width: 150px;
            text-align: center;
        }
        
        .form-input:focus {
            outline: none;
            border-color: #667eea;
        }
        
        .operator-select {
            padding: 15px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 18px;
            background: white;
            cursor: pointer;
        }
        
        .calculate-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 15px 40px;
            border-radius: 8px;
            font-size: 18px;
            cursor: pointer;
            transition: transform 0.2s;
        }
        
        .calculate-btn:hover {
            transform: translateY(-2px);
        }
        
        .result-display {
            margin-top: 20px;
            padding: 20px;
            background: #e8f5e8;
            border: 2px solid #4caf50;
            border-radius: 8px;
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            color: #2e7d32;
        }
        
        .history-section {
            width: 350px;
            background: #2c3e50;
            color: white;
            padding: 30px;
        }
        
        .history-header {
            border-bottom: 2px solid #34495e;
            padding-bottom: 15px;
            margin-bottom: 20px;
        }
        
        .stats-box {
            background: rgba(255,255,255,0.1);
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
        }
        
        .stats-number {
            font-size: 24px;
            font-weight: bold;
            color: #3498db;
        }
        
        .history-list {
            max-height: 300px;
            overflow-y: auto;
        }
        
        .history-item {
            background: rgba(255,255,255,0.05);
            padding: 12px;
            border-radius: 6px;
            margin-bottom: 8px;
            border-left: 3px solid #3498db;
        }
        
        .history-calculation {
            font-family: 'Courier New', monospace;
            font-size: 16px;
            margin-bottom: 5px;
        }
        
        .history-date {
            font-size: 12px;
            color: #bdc3c7;
        }
        
        .no-history {
            text-align: center;
            color: #7f8c8d;
            font-style: italic;
            padding: 20px;
        }
        
        .demo-info {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            color: #856404;
            padding: 15px;
            margin: 20px;
            border-radius: 8px;
            text-align: center;
        }
        
        @media (max-width: 768px) {
            .main-content {
                flex-direction: column;
            }
            
            .history-section {
                width: 100%;
            }
            
            .form-row {
                flex-wrap: wrap;
            }
            
            .form-input {
                width: 120px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚀 Calculator with Database</h1>
            <div>
                <span class="badge">Session: <%= sessionId %></span>
                <span class="badge"><%= hasDatabase ? 'Database Connected' : 'Demo Mode' %></span>
            </div>
        </div>
        
        <% if (!hasDatabase) { %>
        <div class="demo-info">
            <strong>Demo Mode:</strong> Running without database connection. 
            Connect to Azure SQL Database to enable calculation history and persistence.
        </div>
        <% } %>
        
        <div class="main-content">
            <div class="calculator-section">
                <h2>🧮 Calculator</h2>
                
                <% if (typeof result !== 'undefined') { %>
                <div class="result-display">
                    <%= lastCalculation %> = <%= result %>
                    <% if (hasDatabase) { %>
                    <br><small>✅ Saved to database</small>
                    <% } %>
                </div>
                <% } %>
                
                <form method="post" action="/calculate" class="calculator-form">
                    <div class="form-row">
                        <input name="num1" type="number" step="any" required 
                               placeholder="First number" class="form-input">
                        
                        <select name="op" class="operator-select">
                            <option value="+">➕ Add</option>
                            <option value="-">➖ Subtract</option>
                            <option value="*">✖️ Multiply</option>
                            <option value="/">➗ Divide</option>
                        </select>
                        
                        <input name="num2" type="number" step="any" required 
                               placeholder="Second number" class="form-input">
                    </div>
                    
                    <div class="form-row">
                        <button type="submit" class="calculate-btn">
                            🧮 Calculate
                        </button>
                    </div>
                </form>
            </div>

            <div class="history-section">
                <div class="history-header">
                    <h3>📊 Calculation History</h3>
                </div>
                
                <div class="stats-box">
                    <div class="stats-number" id="totalCalcs">-</div>
                    <div>Total Calculations</div>
                    <div style="margin-top: 5px;">
                        <span id="daysActive">-</span> days active
                    </div>
                </div>
                
                <div class="history-list">
                    <% if (hasDatabase && history && history.length > 0) { %>
                        <% history.forEach(calc => { %>
                            <div class="history-item">
                                <div class="history-calculation"><%= calc.DisplayText %></div>
                                <div class="history-date"><%= calc.FormattedDate %></div>
                            </div>
                        <% }); %>
                    <% } else if (hasDatabase) { %>
                        <div class="no-history">
                            No calculations yet.<br>
                            Start calculating to see your history!
                        </div>
                    <% } else { %>
                        <div class="no-history">
                            History available when<br>
                            database is connected
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Load statistics
        function loadStats() {
            fetch('/api/stats')
                .then(response => response.json())
                .then(stats => {
                    document.getElementById('totalCalcs').textContent = stats.TotalCalculations || 0;
                    document.getElementById('daysActive').textContent = stats.DaysActive || 0;
                })
                .catch(err => {
                    console.log('Stats not available:', err);
                    document.getElementById('totalCalcs').textContent = '0';
                    document.getElementById('daysActive').textContent = '0';
                });
        }

        // Load stats on page load
        loadStats();
        
        // Auto-focus first input
        document.querySelector('input[name="num1"]').focus();
    </script>
</body>
</html>